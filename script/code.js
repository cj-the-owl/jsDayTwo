let subjects = ['Python', 'C++', 'VB.Net', 'Javascript', 'Assembly'];
let subject = subjects.at(4);
console.log(subject)
document.write(`<h3>${subject}</h3> <br> <p>The above is the last item within my array and below is the original array</p> <br> <h1 style=color:blue>${subjects}</h1>`)

let middle = subjects.at(2);
console.log(middle)
document.write(middle)